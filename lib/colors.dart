import 'package:flutter/material.dart';

final primaryColor = Colors.purple[50];
final accentColor = Colors.purple[800];
final fillColor = Colors.purple[100];
final darkTextColor = Colors.grey[900];
final darkIconColor = Colors.grey[900];
final lightTextColor = Colors.grey[50];
final lightIconColor = Colors.grey[50];
final errorColor = Colors.red[500];
/*
Scaffold(
      // TODO: Add app bar (102)
      appBar: AppBar(
        // todo: add conditional statement to turn elevation on
        elevation: _showElevation ? 5.0 : 0.0,
        brightness: Brightness.light,
        leading: IconButton(
          icon: Icon(
            Icons.menu,
            semanticLabel: "menu",
          ),
          onPressed: () {
            print("menu button");
          },
        ),
        // Todo: add buttons and title (102)
        title: Text("SHRINE"),
        // TODO: add trailing buttons (102)
        actions: <Widget>[
          IconButton(
            icon: Icon(
              Icons.search,
              semanticLabel: "search",
            ),
            onPressed: () {
              print("my search button");
              Navigator.push(
                  context,
                  MaterialPageRoute<void>(
                    builder: (BuildContext context) => LoginPage(),
                    fullscreenDialog: true,
                  ));
            },
          ),
          IconButton(
            onPressed: () {
              print("filter button");
            },
            icon: Icon(
              Icons.tune,
              semanticLabel: "filter",
            ),
          ),
        ],
      ),
 */
